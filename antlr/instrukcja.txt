1. Wrzuæ ten folder najlepiej na C:, albo zostaw sobie w folderze repozytorium git
2. W "System/Zaawansowane ustawienia systemu" kliknij w Zmienne Œrodowiskowe, dodaj zmienn¹ classpath, je¿eli jej nie ma, i w wartoœci wpisz œcie¿kê do tego folderu gdzie znajduje siê antlr-3.3-complete np. C:\antlr\
3. SprawdŸ czy w zmiennej systemowej path masz œcie¿kê do javac, np. C:\Program Files\Java\jdk1.7.0_17\bin\, jak nie masz to dodaj
4. Uruchom cmd
5. PrzejdŸ do tego folderu
6. Aby stworzyæ lekser i parser na podstawie gramatyki wpisz 
	java  -classpath antlr-3.3-complete.jar org.antlr.Tool CMinus.g
7. Aby skompilowaæ wszystkie .java w folderze wpisz
	javac -classpath antlr-3.3-complete.jar *.java
8. Aby uruchomiæ Main w którym nastêpuje t³umaczenie wpisz
	java -cp ./;antlr-3.3-complete.jar Main Python.stg input
9. Translator ³adnie wypisuje